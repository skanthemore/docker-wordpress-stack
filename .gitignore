# Local environment
.env
!.env.example

# WordPress document root: created by the bind mount at runtime and never
# tracked. The official image installs core here on first start.
/web/

# OS / logs
.DS_Store
*.log
